import java.util.*;

public class practice2 {
    public static void main(String arg[]) {
        Scanner sc = new Scanner(System.in);
        /*
         * int a = sc.nextInt();
         * int b = sc.nextInt();
         * int c = sc.nextInt();
         * float avg = (a+b+c)/3;
         * System.out.println(avg);
         * 
         * float length = sc.nextFloat();
         * float area = length*length;
         * System.out.println(area);
         * 
         * 
         * System.out.print("Cost of pensil is:");
         * 
         * float pensilprice = sc.nextFloat();
         * 
         * System.out.print("Cost of pen is:");
         * 
         * float penprice = sc.nextFloat();
         * 
         * System.out.print("Cost of eraser is:");
         * 
         * float eraserprice = sc.nextFloat();
         * 
         * float totalcost = pensilprice + penprice + eraserprice;
         * 
         * System.out.println("Total cost of pensil,pen and eraser  without GST is : " +
         * totalcost);
         * 
         * float netcost = ((pensilprice * 0.18f ) + (penprice * 0.18f) + (eraserprice *
         * 0.18f)+totalcost);
         * 
         * System.out.println("Net cost with GST of pensil,pen and eraser is : " +
         * netcost);
         */

        // int x = 10, y = 5;
        // int exp1 = (y * (x / y + x / y));
        // int exp2 = (y * x / y + y * x / y);
        // System.out.println(exp1);
         System.out.println(1 + 2 + "3" + 4 + 5);

        sc.close();
    }
}